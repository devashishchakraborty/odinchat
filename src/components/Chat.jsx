import { useState } from "react";
import { Link } from "react-router-dom";

const Chat = ({user}) => {
  const [users, setUsers] = useState([]);
  return (
    <main>
      <section>

      </section>
    </main>
  );
};

export default Chat;
